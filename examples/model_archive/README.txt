MIIND Model Archive

Examples of neural models which can be simulated in MIIND. Unless otherwise stated in a separate README.txt, each directory holds a single .xml simulation file and required supporting files. Each can be run with the command:

$ python -m miind.run <simulation_file>.xml


